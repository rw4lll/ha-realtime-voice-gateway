# Python dependencies for test scripts

# Wyoming protocol library for validation
wyoming>=1.5.0

# Audio bridge test (laptop mic/speaker testing)
pyaudio>=0.2.13

# Note: On macOS, you may need to install portaudio first:
#   brew install portaudio
#   pip install pyaudio
#
# On Linux:
#   sudo apt-get install portaudio19-dev python3-pyaudio
#   pip install pyaudio

